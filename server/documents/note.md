# 1. Install MySql
```
  brew install mysql
  brew services start mysql
  mysql --version
  mysql -u root (mở terminal)

  create database yt_airline_location_db;
```

Booking Service orchestrates flight bookings and manages Booking, Passenger, and Ticket records